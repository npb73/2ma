/** Balls destroyed on each side of an exploding explosive ball. */
export const EXPLOSIVE_BLAST_RADIUS = 5;

export const ICE_FREEZE_SEC = 5;

/** Stones pushed onto the target chain by a volun clear. */
export const VOLUN_STONE_COUNT = 5;

/** Stone lifetime on the path before it despawns. */
export const STONE_LIFETIME_SEC = 30;
